---
description: Commit all changes and push to main
---

1. Stage all changes
// turbo
2. Run command: `git add .`

3. Commit changes (You should ask the user for a commit message, or use a default)
4. Run command: `git commit -m "Update"`

5. Push to remote
// turbo
6. Run command: `git push origin main`
